# Changelog

## Unreleased

- Initialized the project as a Git + GSD workspace.
- Added a real `src/pdfmd` package and began moving product code behind thin skill-script wrappers.
- Added root operator commands via `Makefile`.
- Added status and doctor tooling for quick operational visibility.
- Added standardized run manifests for bundle generation, quality gates, challenge-corpus smoke runs, and backend comparisons.
- Added package/import, wrapper-parity, status/doctor, manifest-contract, and split-path regression tests.
