---
phase: 04-restore-the-canonical-holdout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pdfmd/benchmarks/calibration.py
  - src/pdfmd/benchmarks/embedding_space.py
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/common/runtime.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The canonical why-ethics gate must complete through calibrated local runtime defaults, not through ad hoc timeout overrides"
    - "Apple embedding helper runtime behavior must be measurable, cleanup-aware, and repeatable on the local M4"
    - "Phase 04 cannot claim holdout recovery until the gate can run to completion with trustworthy runtime metadata"
  artifacts:
    - path: "src/pdfmd/benchmarks/calibration.py"
      provides: "Deterministic timeout-calibration logic for the Apple embedding helper on the canonical why-ethics benchmark"
      min_lines: 60
    - path: "src/pdfmd/gates/quality_gate.py"
      provides: "Canonical gate runtime integration that uses calibrated timeout inputs and preserves helper cleanup diagnostics"
      min_lines: 260
    - path: "skills/pdf-to-structured-markdown/tests/test_project_ops.py"
      provides: "Regression coverage for calibration artifacts, runtime summaries, and no-orphan-helper reporting"
      min_lines: 280
  key_links:
    - from: "src/pdfmd/benchmarks/calibration.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "quality-gate runtime defaults are derived from measured calibration output rather than hard-coded guesses"
      pattern: "timeout"
    - from: "src/pdfmd/common/runtime.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "helper cleanup outcomes remain authoritative during gate retries and calibration"
      pattern: "cleanup"
---

<objective>
Make the local runtime trustworthy enough for the canonical holdout gate to complete meaningfully: calibrate the Apple embedding timeout on the M4, wire the calibrated defaults into the gate path, and preserve explicit cleanup diagnostics.

Purpose: remove runtime-blocked ambiguity before any why-ethics content repair begins.
Output: calibrated Apple embedding timeout flow, trusted runtime metadata, and one canonical gate path that no longer depends on manual timeout guessing
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-restore-the-canonical-holdout/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibrate the Apple embedding timeout from real why-ethics runs</name>
  <files>src/pdfmd/benchmarks/calibration.py, src/pdfmd/benchmarks/embedding_space.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Use the existing calibration path to measure the Apple embedding helper against the real why-ethics benchmark and make the timeout recommendation reproducible.

At minimum:
- run the embedding evaluator 5 times against the canonical why-ethics benchmark
- record wall-clock durations and success/failure outcomes
- derive a stable timeout recommendation from the measured runs
- ensure the calibration artifact is explicit about:
  - helper command
  - recommended timeout
  - sample durations
  - cleanup results
  - stale vs fresh calibration state

Do not weaken the gate thresholds in this task. This is runtime calibration, not a re-baselining step.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/calibrate_embedding_timeout.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-retrieval-benchmark.json --runs 5
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_project_ops.py' -v
```
Verify that calibration produces a machine-readable recommendation and cleanup-aware runtime summary without leaving orphaned helper processes behind.
  </verify>
  <done>The Apple embedding helper has a measured timeout recommendation and calibration artifacts are trustworthy</done>
</task>

<task type="auto">
  <name>Task 2: Make the canonical gate consume calibrated runtime defaults</name>
  <files>src/pdfmd/gates/quality_gate.py, src/pdfmd/common/runtime.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Update the quality gate so the canonical why-ethics run uses the calibrated timeout path by default rather than depending on a temporary manual override.

Specifically:
- use the freshest valid calibration artifact when present
- keep explicit CLI override support for debugging
- preserve runtime classification and cleanup reporting
- keep local Apple embedding as the canonical backend

Do not change holdout thresholds, probe limits, retrieval baselines, or embedding baselines in this task.
  </action>
  <verify>
Run:
```bash
make gate
make doctor
```
Verify that the gate reaches the embedding stage with calibrated defaults and that runtime failures, if any remain, are still classified cleanly rather than cascading.
  </verify>
  <done>The canonical gate path uses measured runtime defaults and remains diagnostically trustworthy</done>
</task>

</tasks>

<verification>
- A 5-run calibration pass exists for the canonical why-ethics bundle
- The quality gate consumes calibrated runtime defaults instead of guessed 5-second helper settings
- Runtime artifacts still classify failures and record cleanup outcomes explicitly
- No orphaned helper-process behavior is introduced by calibration work
</verification>

<success_criteria>
- The canonical gate can attempt a full why-ethics run using calibrated local runtime defaults
- Timeout selection is measurable and reproducible
- Runtime metadata remains authoritative and cleanup-aware
- Phase 04 work starts from a trustworthy gate runtime rather than a blocked shortcut
</success_criteria>

<output>
After completion, create `.planning/phases/04-restore-the-canonical-holdout/04-01-SUMMARY.md`
</output>
