---
phase: 04-restore-the-canonical-holdout
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/pdfmd/convert/layout.py
  - src/pdfmd/convert/rag.py
  - src/pdfmd/convert/render.py
  - src/pdfmd/convert/output.py
  - skills/pdf-to-structured-markdown/references/why-ethics-regressions.json
  - skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py
  - skills/pdf-to-structured-markdown/tests/test_regression_scopes.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phase 04 repairs are why-ethics holdout restorations, not a general cross-book heuristic wave"
    - "Any repair that has multiple plausible implementations must choose the simplest passing variant, not the most elaborate one"
    - "Probe restoration and spatial retrieval restoration must happen without regressing structural audit, existing strict regressions, or the clean Specters of Marx control"
  artifacts:
    - path: "skills/pdf-to-structured-markdown/references/why-ethics-regressions.json"
      provides: "Strict holdout regressions for the repaired why-ethics cases"
      min_lines: 80
    - path: "src/pdfmd/convert/rag.py"
      provides: "Bounded holdout repair logic for the why-ethics probe drift cases"
      min_lines: 220
    - path: "src/pdfmd/convert/output.py"
      provides: "Spatial main/supplement output restoration needed to recover the frozen retrieval baseline"
      min_lines: 120
  key_links:
    - from: "skills/pdf-to-structured-markdown/references/why-ethics-regressions.json"
      to: "src/pdfmd/convert/rag.py"
      via: "new strict regressions lock the repaired holdout cases once the output is corrected"
      pattern: "why-ethics"
    - from: "src/pdfmd/convert/output.py"
      to: "src/pdfmd/benchmarks/retrieval.py"
      via: "restored spatial corpus ownership feeds the exact retrieval runs that regressed"
      pattern: "spatial_main_plus_supplement"
---

<objective>
Restore the frozen why-ethics holdout outputs by applying the smallest bounded repairs that clear the current probe drift and spatial_main_plus_supplement retrieval regressions.

Purpose: get the canonical holdout back to green before Phase 05 resumes broader extractor work.
Output: repaired why-ethics content/output behavior, promoted strict holdout regressions, and restored frozen probe/retrieval baselines
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-restore-the-canonical-holdout/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the minimal why-ethics repair for probe drift and spatial retrieval drift</name>
  <files>src/pdfmd/convert/layout.py, src/pdfmd/convert/rag.py, src/pdfmd/convert/render.py, src/pdfmd/convert/output.py</files>
  <action>
Use the characterization from Plan 02 to restore the current why-ethics deltas with the smallest bounded repair that works.

Allowed work in this plan:
- repair the exact lowercase-start, dangling-end, and repeated-word holdout cases above the frozen baseline
- repair the exact spatial_main_plus_supplement ownership/content drift that caused the current retrieval regression
- compare bounded repair alternatives only if there is more than one plausible route, using the existing comparison tooling and removing losing toggles after selection

Not allowed in this plan:
- cross-book heuristic expansion
- baseline changes
- re-opening ToC logic, filename logic, or remote-backend policy
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/probe_artifacts.py generated/why-ethics
python3 skills/pdf-to-structured-markdown/scripts/evaluate_retrieval.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-retrieval-benchmark.json --profiles fielded_bm25,chargram_tfidf,fused_rrf
make smoke
```
Verify that why-ethics probe counts return to the frozen ceiling, the spatial_main_plus_supplement retrieval runs match baseline, and Specters of Marx remains unchanged.
  </verify>
  <done>The canonical why-ethics content/output drift is repaired without broadening the heuristic surface</done>
</task>

<task type="auto">
  <name>Task 2: Promote repaired why-ethics cases into strict holdout regressions</name>
  <files>skills/pdf-to-structured-markdown/references/why-ethics-regressions.json, skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py, skills/pdf-to-structured-markdown/tests/test_regression_scopes.py</files>
  <action>
Once the repaired why-ethics outputs are correct, promote the relevant holdout cases into strict regression coverage.

At minimum:
- add or tighten the why-ethics regression entries for the repaired cases
- keep existing strict regressions green
- make the unit tests reflect the repaired behavior, not the former broken output

This is the point where the holdout truth becomes enforceable rather than descriptive.
  </action>
  <verify>
Run:
```bash
make test-fast
python3 skills/pdf-to-structured-markdown/scripts/check_regressions.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-regressions.json --strict
```
Verify that the repaired why-ethics cases are now protected by strict regressions and that no existing holdout checks regress.
  </verify>
  <done>Repaired why-ethics cases are protected by strict regression coverage</done>
</task>

</tasks>

<verification>
- Why-ethics probe count returns to the frozen limit or better
- Spatial_main_plus_supplement retrieval returns to the frozen baseline exactly
- Existing why-ethics strict regressions remain green while new repaired cases are promoted
- Specters of Marx remains unchanged as the negative control
</verification>

<success_criteria>
- The canonical why-ethics holdout content drift is repaired
- Frozen probe and retrieval baselines are restored
- Strict regression coverage now includes the repaired holdout cases
- No broader cross-book heuristic wave is smuggled into Phase 04
</success_criteria>

<output>
After completion, create `.planning/phases/04-restore-the-canonical-holdout/04-03-SUMMARY.md`
</output>
