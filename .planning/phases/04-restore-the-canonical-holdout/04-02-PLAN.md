---
phase: 04-restore-the-canonical-holdout
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pdfmd/gates/probe.py
  - src/pdfmd/benchmarks/retrieval.py
  - skills/pdf-to-structured-markdown/tests/fixtures/why_ethics_holdout_cases.json
  - skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py
  - skills/pdf-to-structured-markdown/tests/test_regression_scopes.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Why-ethics probe and retrieval drift must be explained by exact holdout cases before extractor behavior changes are accepted"
    - "Characterization comes before repair: the failing holdout cases become fixtures and diagnostics before implementation begins"
    - "The phase must distinguish user-visible extraction defects from evaluator/runtime noise"
  artifacts:
    - path: "skills/pdf-to-structured-markdown/tests/fixtures/why_ethics_holdout_cases.json"
      provides: "Real why-ethics holdout cases for the current probe and spatial-retrieval deltas"
      min_lines: 30
    - path: "src/pdfmd/gates/probe.py"
      provides: "Exact offending scope diagnostics for the current probe drift rather than aggregate counts only"
      min_lines: 160
    - path: "src/pdfmd/benchmarks/retrieval.py"
      provides: "Per-case retrieval miss diagnostics for spatial_main_plus_supplement baseline regressions"
      min_lines: 260
  key_links:
    - from: "skills/pdf-to-structured-markdown/tests/fixtures/why_ethics_holdout_cases.json"
      to: "skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py"
      via: "real holdout examples are locked as characterization fixtures before repair"
      pattern: "why_ethics"
    - from: "src/pdfmd/gates/probe.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "quality-gate failures can be traced to exact offending scopes instead of opaque aggregate counts"
      pattern: "scope"
    - from: "src/pdfmd/benchmarks/retrieval.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "retrieval regressions identify the exact spatial entries and misses that need restoration"
      pattern: "spatial_main_plus_supplement"
---

<objective>
Characterize the exact why-ethics holdout deltas before code repair: surface the specific probe cases and spatial retrieval misses, then lock them into repeatable fixtures and diagnostics.

Purpose: make Phase 04 repairs evidence-driven and minimal rather than heuristic-first.
Output: explicit why-ethics delta packet, real holdout fixtures, and per-case diagnostics for probe/retrieval drift
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-restore-the-canonical-holdout/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface exact probe-drift and spatial-retrieval cases for why-ethics</name>
  <files>src/pdfmd/gates/probe.py, src/pdfmd/benchmarks/retrieval.py</files>
  <action>
Extend the current diagnostic surfaces so the why-ethics hard-gate failures are attributable to exact content cases.

At minimum:
- map the extra probe counts to exact leaf/path/block/page cases for:
  - repeated_adjacent_word
  - rag_block_lowercase_start
  - rag_block_dangling_end
- map the spatial_main_plus_supplement retrieval regressions to the exact cases where rank or top-1 changed
- keep backward-compatible report schemas, adding per-case detail without removing the aggregate summaries

Do not change extractor behavior in this task. This is characterization only.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/probe_artifacts.py generated/why-ethics
python3 skills/pdf-to-structured-markdown/scripts/evaluate_retrieval.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-retrieval-benchmark.json --profiles fielded_bm25,chargram_tfidf,fused_rrf
```
Verify that both outputs surface exact why-ethics cases for the current drift, not just aggregate counts.
  </verify>
  <done>Probe and retrieval deltas are attributable to exact why-ethics cases</done>
</task>

<task type="auto">
  <name>Task 2: Lock the current why-ethics deltas into characterization fixtures</name>
  <files>skills/pdf-to-structured-markdown/tests/fixtures/why_ethics_holdout_cases.json, skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py, skills/pdf-to-structured-markdown/tests/test_regression_scopes.py</files>
  <action>
Add real why-ethics holdout fixtures for the currently failing cases so the next plan can repair them deliberately.

The fixtures should capture:
- the exact extra probe cases above the frozen baseline
- the exact spatial retrieval misses for the regressed runs
- enough provenance to locate the affected bundle content without relying on memory

Keep these as characterization fixtures in this plan. Promotion into strict passing holdout regressions happens in the repair plan once the outputs are corrected.
  </action>
  <verify>
Run:
```bash
make test-fast
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_prose_fragment_repairs.py' -v
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_regression_scopes.py' -v
```
Verify that the characterization fixtures are stable and point to the intended holdout cases.
  </verify>
  <done>Real why-ethics holdout cases are captured as fixtures and can guide the repair slice without guesswork</done>
</task>

</tasks>

<verification>
- The current why-ethics probe increases are attributable to exact cases
- The spatial_main_plus_supplement retrieval regressions are attributable to exact cases
- Real holdout fixtures exist before extractor code is changed
- Report schemas remain backward-compatible while gaining per-case detail
</verification>

<success_criteria>
- Phase 04 repairs can target named why-ethics cases instead of aggregate symptoms
- Characterization fixtures make the holdout drift repeatable
- Diagnostics separate user-visible extraction defects from runtime/evaluator noise
- No extractor behavior changes are smuggled into the characterization slice
</success_criteria>

<output>
After completion, create `.planning/phases/04-restore-the-canonical-holdout/04-02-SUMMARY.md`
</output>
