---
phase: 07-infrastructure-alignment-and-live-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/pdfmd/benchmarks/embedding_space.py
  - src/pdfmd/benchmarks/remote_backends.py
  - skills/pdf-to-structured-markdown/references/remote-backends.json
  - Makefile
  - skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py
autonomous: false

must_haves:
  truths:
    - "Models requiring trust_remote_code=True (nomic-embed, stella) load successfully through the evaluation pipeline"
    - "Operator can run make compare-backends without --dry-run and get a comparison-summary.json with real metric values"
    - "Dry-run is still available via COMPARE_BACKENDS_ARGS='--dry-run' for operators who want it"
    - "At least bge-small-en-v1.5 produces real metrics in the live pipeline test"
  artifacts:
    - path: "src/pdfmd/benchmarks/embedding_space.py"
      provides: "SentenceTransformer constructor accepts trust_remote_code when needed"
      contains: "trust_remote_code"
    - path: "src/pdfmd/benchmarks/remote_backends.py"
      provides: "trust_remote_code threaded from config through evaluation command"
      contains: "trust_remote_code"
    - path: "skills/pdf-to-structured-markdown/references/remote-backends.json"
      provides: "Per-model trust_remote_code config field"
    - path: "Makefile"
      provides: "compare-backends target runs live by default"
  key_links:
    - from: "skills/pdf-to-structured-markdown/references/remote-backends.json"
      to: "src/pdfmd/benchmarks/remote_backends.py"
      via: "per-model config with optional trust_remote_code field"
      pattern: "trust_remote_code"
    - from: "src/pdfmd/benchmarks/remote_backends.py"
      to: "src/pdfmd/benchmarks/embedding_space.py"
      via: "--trust-remote-code CLI flag threaded through evaluation command"
      pattern: "trust.remote.code"
    - from: "Makefile"
      to: "src/pdfmd/benchmarks/remote_backends.py"
      via: "compare-backends target without --dry-run"
      pattern: "compare-backends.*COMPARE_BACKENDS_ARGS"
---

<objective>
Add trust_remote_code support for models that need it (EMBED-04) and enable live pipeline execution by removing the --dry-run default from the Makefile (EMBED-01). Verify with a human-confirmed live smoke test against dionysus.

Purpose: This plan completes Phase 07 by making the pipeline capable of running models like nomic-embed that require custom code loading, and by proving the full pipeline works end-to-end with real GPU metrics.
Output: trust_remote_code support in the evaluation path, live Makefile target, and human-verified live comparison output.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-infrastructure-alignment-and-live-pipeline/07-CONTEXT.md
@.planning/phases/07-infrastructure-alignment-and-live-pipeline/07-01-SUMMARY.md

@src/pdfmd/benchmarks/remote_backends.py
@src/pdfmd/benchmarks/embedding_space.py
@skills/pdf-to-structured-markdown/references/remote-backends.json
@Makefile
@skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trust_remote_code support and enable live pipeline execution</name>
  <files>
    src/pdfmd/benchmarks/embedding_space.py
    src/pdfmd/benchmarks/remote_backends.py
    skills/pdf-to-structured-markdown/references/remote-backends.json
    Makefile
    skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py
  </files>
  <action>
**EMBED-04: Add trust_remote_code support** through the full evaluation path. Use per-model config (Claude's discretion from CONTEXT.md) since only specific models need it and making it always-on is unnecessarily permissive.

1. **remote-backends.json**: Add an optional `"model_config"` object alongside the `"models"` array. This maps model names to per-model settings. For the current 3 models, none need it, but the schema should support it:
   ```json
   "model_config": {
     "nomic-ai/nomic-embed-text-v1.5": {"trust_remote_code": true}
   }
   ```
   Do NOT add nomic to the models list yet (deferred to Phase 08). Just establish the config schema.

2. **validate_backend_entry()** in `remote_backends.py` (~line 191): Accept optional `"model_config"` field. Validate it as a dict of model-name -> settings if present. Pass it through in the validated output. Do NOT add it to `required_fields`.

3. **build_remote_evaluation_command()** in `remote_backends.py` (~line 454): Accept a new `trust_remote_code: bool = False` parameter. When True, append `"--trust-remote-code"` to the evaluation command list (before the `>` redirect).

4. **Main pipeline loop** in `remote_backends.py` (~line 1088): When building the evaluation command for each model, look up `model_config.get(model_name, {}).get("trust_remote_code", False)` from the backend config and pass it to `build_remote_evaluation_command()`.

5. **embedding_space.py**:
   - Add `--trust-remote-code` as a `store_true` CLI argument in `parse_args()`.
   - In `load_embeddings_sentence_transformers()` (~line 658): Add a `trust_remote_code: bool = False` parameter.
   - At line 677 where `SentenceTransformer(model_name, device=resolved_device)` is called: pass `trust_remote_code=trust_remote_code` as a keyword argument. The `sentence_transformers.SentenceTransformer` constructor accepts this parameter directly.
   - Wire `args.trust_remote_code` through to the function call from wherever `load_embeddings_sentence_transformers` is invoked.

6. **Test additions** in `test_remote_embedding_backends.py`:
   - Test that `build_remote_evaluation_command` with `trust_remote_code=True` includes `--trust-remote-code` in the generated SSH command
   - Test that `build_remote_evaluation_command` with `trust_remote_code=False` (default) does NOT include it
   - Test that `validate_backend_entry` passes with `model_config` present and absent
   - Test that `parse_args` in `evaluate_embedding_space` recognizes `--trust-remote-code`

**EMBED-01: Enable live pipeline execution.**

7. **Makefile** line 39: Change:
   ```
   $(PYTHON) $(SCRIPTS_DIR)/compare_embedding_backends.py $(WHY_ETHICS_BUNDLE) $(BENCHMARK_JSON) --dry-run $(COMPARE_BACKENDS_ARGS)
   ```
   To:
   ```
   $(PYTHON) $(SCRIPTS_DIR)/compare_embedding_backends.py $(WHY_ETHICS_BUNDLE) $(BENCHMARK_JSON) $(COMPARE_BACKENDS_ARGS)
   ```
   This removes `--dry-run` so `make compare-backends` runs live by default (per locked decision). Operators who want dry-run can pass `make compare-backends COMPARE_BACKENDS_ARGS='--dry-run'`.

8. In `remote_backends.py`, update the `--backend-ids` handling: when running live with the existing 3 models, the operator should be able to do `make compare-backends COMPARE_BACKENDS_ARGS='--backend-ids dionysus'` to run just the remote backend. Verify this already works (it should from existing code).
  </action>
  <verify>
Run `make test-fast` -- passes. Run `python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_remote_embedding_backends.py' -v` -- all tests pass including new trust_remote_code tests. Verify the Makefile no longer contains `--dry-run` in the compare-backends target. Verify `remote-backends.json` has the model_config schema.
  </verify>
  <done>
trust_remote_code flows from remote-backends.json per-model config through build_remote_evaluation_command to embedding_space.py's SentenceTransformer constructor. Makefile compare-backends target runs live by default. New tests cover trust_remote_code threading and config validation. All existing tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live pipeline execution on dionysus</name>
  <what-built>
Complete infrastructure alignment (pin fixes, SSH timeouts, VRAM guards, trust_remote_code support) and live pipeline enablement. The pipeline should now be able to run a real embedding comparison against dionysus without --dry-run.
  </what-built>
  <how-to-verify>
1. Run a single-model live comparison to validate the pipeline end-to-end:
   ```bash
   make compare-backends COMPARE_BACKENDS_ARGS='--backend-ids dionysus'
   ```
   This will:
   - Run the local Apple NL baseline on the Mac
   - SSH to dionysus, bootstrap the venv, evaluate bge-small-en-v1.5 (and bge-base, e5-base)
   - Fetch results back and produce comparison-summary.json

2. Verify the output at `generated/embedding-backend-comparison/<run-id>/comparison-summary.json`:
   - `dry_run` should be `false`
   - At least one result should have `status: "success"` with real `aggregate_metrics` (non-null twin_cosine, hit@1, MRR values)
   - Check that `wall_clock_seconds` shows real elapsed time (not 0)

3. If any model times out, check the runtime.json for that model -- it should show `status: "timeout"` with a clear error message rather than hanging indefinitely.

4. Verify VRAM probe results appear in the runtime data (look for `vram_pre` entries in the backend shared runtime).

5. If the live run fails due to SSH connectivity or dionysus being offline, that is NOT a pipeline bug -- verify the error message is clear and actionable.
  </how-to-verify>
  <resume-signal>Type "approved" if comparison-summary.json contains real metrics, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
- `make test-fast` passes
- All remote embedding backend tests pass
- Makefile compare-backends runs live by default
- trust_remote_code flows through the full evaluation path
- Live comparison produces real metrics from at least one model on dionysus
- Timeout and VRAM safety mechanisms are observable in runtime artifacts
</verification>

<success_criteria>
1. Models requiring trust_remote_code=True are supported by the evaluation pipeline (EMBED-04)
2. Operator can run make compare-backends without --dry-run and get real metrics (EMBED-01)
3. Live comparison-summary.json has non-null metric values from at least bge-small-en-v1.5
4. Dry-run remains available via COMPARE_BACKENDS_ARGS for operators who want it
5. All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-infrastructure-alignment-and-live-pipeline/07-02-SUMMARY.md`
</output>
