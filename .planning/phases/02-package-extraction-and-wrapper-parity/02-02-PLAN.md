---
phase: 02-package-extraction-and-wrapper-parity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pdfmd/benchmarks/__init__.py
  - src/pdfmd/benchmarks/embedding_space.py
  - src/pdfmd/benchmarks/retrieval.py
  - src/pdfmd/benchmarks/remote_backends.py
  - src/pdfmd/benchmarks/variant_comparison.py
  - src/pdfmd/benchmarks/calibration.py
  - src/pdfmd/cli/evaluate_embedding_space.py
  - src/pdfmd/cli/evaluate_retrieval.py
  - src/pdfmd/cli/compare_embedding_backends.py
  - src/pdfmd/cli/compare_variants.py
  - src/pdfmd/cli/calibrate_embedding_timeout.py
  - skills/pdf-to-structured-markdown/tests/test_embedding_projections.py
  - skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Benchmark logic lives under `src/pdfmd/benchmarks/` rather than primarily inside `src/pdfmd/cli/*.py`"
    - "CLI entrypoints for embedding, retrieval, calibration, backend comparison, and variant comparison remain backward-compatible"
    - "The remote experiment backend stays report-only and does not alter the canonical local gate outcome"
  artifacts:
    - path: "src/pdfmd/benchmarks/embedding_space.py"
      provides: "Primary embedding evaluation implementation outside the CLI shim"
      min_lines: 200
    - path: "src/pdfmd/benchmarks/remote_backends.py"
      provides: "Remote backend comparison orchestration outside the CLI shim"
      min_lines: 200
    - path: "skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py"
      provides: "Regression coverage for backend config, manifests, command construction, and dry-run behavior"
      min_lines: 200
  key_links:
    - from: "src/pdfmd/cli/evaluate_embedding_space.py"
      to: "src/pdfmd/benchmarks/embedding_space.py"
      via: "CLI delegates substantive evaluator behavior to the benchmark module"
      pattern: "from pdfmd.benchmarks"
    - from: "src/pdfmd/cli/compare_embedding_backends.py"
      to: "src/pdfmd/benchmarks/remote_backends.py"
      via: "CLI delegates remote backend comparison to the benchmark module"
      pattern: "from pdfmd.benchmarks"
---

<objective>
Move benchmark and experiment logic into `src/pdfmd/benchmarks/` so the CLI layer becomes a thin compatibility surface instead of the main implementation home.

Purpose: make embedding, retrieval, calibration, and backend-comparison behavior easier to reason about, test, and evolve without dragging command-line wiring into every code change.
Output: real benchmark modules behind stable CLI entrypoints
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-extraction-and-wrapper-parity/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract embedding and retrieval evaluators into benchmark modules</name>
  <files>src/pdfmd/benchmarks/embedding_space.py, src/pdfmd/benchmarks/retrieval.py, src/pdfmd/cli/evaluate_embedding_space.py, src/pdfmd/cli/evaluate_retrieval.py</files>
  <action>
Create real benchmark modules for the evaluator logic currently living in:
- `src/pdfmd/cli/evaluate_embedding_space.py`
- `src/pdfmd/cli/evaluate_retrieval.py`

Move substantive implementation into `src/pdfmd/benchmarks/` and leave the CLI files as thin argument-parsing and main-entry shims.

Preserve:
- current JSON output schema
- current benchmark/report semantics
- current Apple vs sentence-transformers backend behavior
- current normalized embedding projection logic

Do not change visible markdown outputs or bundle contracts in this plan.
  </action>
  <verify>
Run:
```bash
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_embedding_projections.py' -v
python3 skills/pdf-to-structured-markdown/scripts/evaluate_embedding_space.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-retrieval-benchmark.json --embedding-backend sentence_transformers --model-name BAAI/bge-small-en-v1.5 --device cpu --top-k 1 --neighbor-k 1 --views body --corpora rag_linearized --reference-corpus rag_linearized
```
If the second command is too heavy on the local machine, use the existing tiny-fixture smoke path from the test suite and verify the CLI still reaches the package implementation without import/path errors.
  </verify>
  <done>Embedding and retrieval evaluators live in `pdfmd.benchmarks`, with CLI shims left in place</done>
</task>

<task type="auto">
  <name>Task 2: Extract backend-comparison and calibration tools into benchmark modules</name>
  <files>src/pdfmd/benchmarks/remote_backends.py, src/pdfmd/benchmarks/variant_comparison.py, src/pdfmd/benchmarks/calibration.py, src/pdfmd/cli/compare_embedding_backends.py, src/pdfmd/cli/compare_variants.py, src/pdfmd/cli/calibrate_embedding_timeout.py, skills/pdf-to-structured-markdown/tests/test_remote_embedding_backends.py</files>
  <action>
Move the remote backend comparison, heuristic variant comparison, and timeout calibration implementations into real `pdfmd.benchmarks` modules.

Keep these invariants:
- local Apple gate remains canonical
- remote SSH/GPU results remain report-only
- dry-run behavior still works
- run manifests remain backward-compatible
- backend selection/report logic remains intact

The CLI files should end this task as entrypoints, not the main home of remote orchestration logic.
  </action>
  <verify>
Run:
```bash
make compare-backends
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_remote_embedding_backends.py' -v
```
Verify that the dry-run comparison succeeds and still writes a comparison summary plus root/per-run manifests.
  </verify>
  <done>Remote benchmark tooling lives under `pdfmd.benchmarks` and dry-run/report-only behavior is preserved</done>
</task>

</tasks>

<verification>
- `src/pdfmd/benchmarks/` contains real implementation modules, not only one-line aliases
- CLI benchmark commands still run through the skill wrappers
- Existing embedding/backend tests stay green
- Dry-run remote comparison still works and produces manifests
</verification>

<success_criteria>
- Benchmark logic has moved out of the CLI layer
- Wrapper CLIs still behave the same from the user’s perspective
- Remote experiments remain optional and report-only
- Existing benchmark tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-extraction-and-wrapper-parity/02-02-SUMMARY.md`
</output>
