---
phase: 02-package-extraction-and-wrapper-parity
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
files_modified:
  - README.md
  - Makefile
  - .planning/STATE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
  - skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The docs and codebase map reflect the post-extraction package boundaries rather than the old monolithic CLI layout"
    - "There is explicit regression coverage for wrapper parity and the root operator surface"
    - "The end-of-phase verification matrix is clear about what must stay green now and what is intentionally still red in later phases"
  artifacts:
    - path: "README.md"
      provides: "Updated at-a-glance operator surface and package layout description"
      min_lines: 40
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Updated subsystem-level architecture after benchmark/gate/convert extraction"
      min_lines: 30
    - path: "skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py"
      provides: "Direct regression coverage for script wrapper to package mapping"
      min_lines: 80
  key_links:
    - from: "Makefile"
      to: "skills/pdf-to-structured-markdown/scripts/"
      via: "root operator commands still execute through the stable script wrapper surface"
      pattern: "skills/pdf-to-structured-markdown/scripts"
    - from: "skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py"
      to: "skills/pdf-to-structured-markdown/scripts/"
      via: "wrapper inventory and import parity are asserted in tests"
      pattern: "load_script_module"
---

<objective>
Close Phase 02 by refreshing visibility artifacts and locking wrapper parity into tests so the package split remains understandable and safe to extend.

Purpose: make the new structure glanceable, keep the command surface stable, and ensure later phases inherit a coherent project map instead of stale docs.
Output: refreshed docs/codebase map, wrapper-parity tests, explicit verification matrix
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-extraction-and-wrapper-parity/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh docs and codebase map to match the extracted package structure</name>
  <files>README.md, .planning/STATE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <action>
Update the root and planning docs so they accurately describe the codebase after Plans 02-04 land.

At minimum, refresh:
- top-level repo map
- subsystem boundaries
- operator commands
- current known red/green verification surfaces
- testing tiers and ownership rules

Prefer concise, factual updates over narrative changelog prose.
  </action>
  <verify>
Run:
```bash
make status
sed -n '1,220p' README.md
sed -n '1,220p' .planning/codebase/ARCHITECTURE.md
```
Verify that the project can be understood from the root/operator docs without reading the implementation first.
  </verify>
  <done>Docs and codebase map reflect the extracted structure at a glance</done>
</task>

<task type="auto">
  <name>Task 2: Add wrapper parity coverage and run the end-of-phase verification matrix</name>
  <files>skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py, Makefile</files>
  <action>
Add direct regression coverage for wrapper parity across the main script surface.

The wrapper test should assert, at minimum:
- each major script wrapper imports the intended `pdfmd.*` package module
- `main()` remains exposed where expected
- root commands still call the stable wrapper layer

Then run the Phase 02 verification matrix:
- `make test-fast`
- `make test`
- `make status`
- `make doctor`
- `make smoke`
- `make compare-backends`
- `make gate`

For `make gate`, the pass condition in this phase is not full green. The pass condition is that the command reaches the real gate implementation and any failure remains in the known `why-ethics` problem classes rather than packaging/path regressions.
  </action>
  <verify>
Capture the command outputs and confirm:
- fast and full test suites pass
- smoke and dry-run backend comparison work
- gate runtime/report artifacts are still produced
- any gate failure is still attributable to the known `why-ethics` runtime/metric issues
  </verify>
  <done>Wrapper parity is protected by tests and the end-of-phase verification matrix has been run against the extracted codebase</done>
</task>

</tasks>

<verification>
- Docs describe the real post-extraction structure
- Wrapper parity has direct regression tests
- The phase verification matrix is explicit and reproducible
- Known red gates are clearly separated from package-split regressions
</verification>

<success_criteria>
- The repo is glanceable after the package extraction
- Wrapper parity is locked in by tests
- Root commands remain the stable operator surface
- Phase 02 can hand off cleanly into Phase 03 without stale structure docs
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-extraction-and-wrapper-parity/02-05-SUMMARY.md`
</output>
