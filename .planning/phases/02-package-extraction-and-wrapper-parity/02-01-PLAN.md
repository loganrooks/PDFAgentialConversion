---
phase: 02-package-extraction-and-wrapper-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pdfmd/common/__init__.py
  - src/pdfmd/common/io.py
  - src/pdfmd/common/manifests.py
  - src/pdfmd/common/runtime.py
  - src/pdfmd/common/paths.py
  - src/pdfmd/ops/doctor.py
  - src/pdfmd/ops/status_snapshot.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
  - README.md
  - Makefile
autonomous: true
requirements: []  # REQUIREMENTS.md is milestone-level prose; no phase-specific IDs yet

must_haves:
  truths:
    - "Package code resolves project, skill, scripts, references, and generated paths through one shared helper layer rather than ad hoc __file__ math"
    - "Manifest validation and writing are centralized so bundle generation, quality gate, challenge corpus, and backend comparison all use the same contract surface"
    - "The operator surface (`make doctor`, `make status`, `make test-fast`) reads from shared helpers and remains stable after the package refactor"
  artifacts:
    - path: "src/pdfmd/common/paths.py"
      provides: "Canonical project, skill, script, reference, and generated-output path helpers"
      min_lines: 40
    - path: "src/pdfmd/common/manifests.py"
      provides: "Validated manifest helpers reused by all runtime report producers"
      min_lines: 20
    - path: "skills/pdf-to-structured-markdown/tests/test_project_ops.py"
      provides: "Regression coverage for shared defaults and operator-surface behavior"
      min_lines: 120
  key_links:
    - from: "src/pdfmd/common/paths.py"
      to: "src/pdfmd/ops/status_snapshot.py"
      via: "status snapshot resolves canonical bundle/report locations through shared helpers"
      pattern: "pdfmd.common.paths"
    - from: "src/pdfmd/common/manifests.py"
      to: "src/pdfmd/cli/run_quality_gate.py"
      via: "quality-gate manifest creation goes through validated helpers"
      pattern: "ensure_manifest_payload"
    - from: "src/pdfmd/common/manifests.py"
      to: "src/pdfmd/cli/compare_embedding_backends.py"
      via: "backend comparison root and per-run manifests use the shared manifest contract"
      pattern: "ensure_manifest_payload"
---

<objective>
Create the shared package foundation that the rest of Phase 02 depends on: one canonical path layer, one manifest contract layer, and one stable operator surface for status/doctor/fast verification.

Purpose: stop the package split from duplicating path logic or silently drifting manifest formats as code moves out of `src/pdfmd/cli/`.
Output: shared path helpers, shared manifest helpers, operator-surface regression tests
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-extraction-and-wrapper-parity/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical shared path helpers and migrate operator callers</name>
  <files>src/pdfmd/common/paths.py, src/pdfmd/ops/doctor.py, src/pdfmd/ops/status_snapshot.py, Makefile</files>
  <action>
Create `src/pdfmd/common/paths.py` as the single package-level source for:
- project root
- skill directory
- scripts directory
- references directory
- generated output root
- canonical why-ethics and challenge-corpus report locations

Then migrate `src/pdfmd/ops/doctor.py` and `src/pdfmd/ops/status_snapshot.py` to use those helpers instead of hard-coded or duplicated path construction.

Keep the public CLI behavior unchanged:
- `python3 skills/pdf-to-structured-markdown/scripts/doctor.py`
- `python3 skills/pdf-to-structured-markdown/scripts/status_snapshot.py`
- `make doctor`
- `make status`

Do not change the visible text layout beyond what is needed to make the data source canonical and reliable.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/doctor.py
python3 skills/pdf-to-structured-markdown/scripts/status_snapshot.py
make status
```
Verify that the commands still resolve the local/remote environments and the latest report artifacts without path errors.
  </verify>
  <done>Shared path helpers exist and operator commands resolve all canonical locations through them</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate manifest validation and add contract regression coverage</name>
  <files>src/pdfmd/common/manifests.py, src/pdfmd/common/io.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py, README.md</files>
  <action>
Extend the shared manifest layer so runtime producers can validate and write manifests through one consistent interface.

At minimum:
- keep `validate_manifest_payload()` and `ensure_manifest_payload()`
- add any small helper needed to reduce repeated write/validation boilerplate
- keep the manifest schemas backward-compatible for:
  - bundle generation
  - quality gate
  - challenge corpus
  - backend comparison

Update `test_project_ops.py` so it explicitly covers:
- path defaults after the package split
- manifest contract validation for all four manifest kinds
- wrapper import parity for the operator surface

Refresh README if needed so the documented operator surface matches the actual command flow.
  </action>
  <verify>
Run:
```bash
make test-fast
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_project_ops.py' -v
```
Verify that manifest contract tests and operator-surface tests pass without special environment setup beyond the current workspace.
  </verify>
  <done>Manifest validation is centralized and project-ops tests lock the shared operator surface in place</done>
</task>

</tasks>

<verification>
- `src/pdfmd/common/paths.py` exists and is imported by the operator-surface modules
- `make doctor`, `make status`, and `make test-fast` all pass
- No path defaults in the operator surface depend on `Path(__file__).parent` pointing at the old skill implementation layout
- Manifest tests explicitly cover all current runtime report types
</verification>

<success_criteria>
- Shared path resolution exists in package code
- Manifest validation is centralized and reused
- Operator-surface commands still work through the wrappers
- Project-ops regression tests prevent package-split path drift
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-extraction-and-wrapper-parity/02-01-SUMMARY.md`
</output>
