---
phase: 02-package-extraction-and-wrapper-parity
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pdfmd/gates/__init__.py
  - src/pdfmd/gates/audit.py
  - src/pdfmd/gates/probe.py
  - src/pdfmd/gates/regressions.py
  - src/pdfmd/gates/review_packet.py
  - src/pdfmd/gates/challenge_corpus.py
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/gates/common.py
  - src/pdfmd/cli/audit_bundle.py
  - src/pdfmd/cli/catalog_anchors.py
  - src/pdfmd/cli/check_regressions.py
  - src/pdfmd/cli/probe_artifacts.py
  - src/pdfmd/cli/render_review_packet.py
  - src/pdfmd/cli/run_challenge_corpus.py
  - src/pdfmd/cli/run_quality_gate.py
  - src/pdfmd/cli/quality_gate_common.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
  - skills/pdf-to-structured-markdown/tests/test_regression_scopes.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Gate/orchestrator logic lives under `src/pdfmd/gates/` rather than primarily in `src/pdfmd/cli/*.py`"
    - "Quality gate, challenge corpus, probe, audit, and regression tools preserve their current report/JSON contracts"
    - "A failing `make gate` fails for product reasons or known Apple-runtime reasons, not because the package split broke path resolution or imports"
  artifacts:
    - path: "src/pdfmd/gates/quality_gate.py"
      provides: "Primary quality-gate implementation outside the CLI shim"
      min_lines: 180
    - path: "src/pdfmd/gates/challenge_corpus.py"
      provides: "Primary challenge-corpus implementation outside the CLI shim"
      min_lines: 140
    - path: "generated/why-ethics/quality-gate/run-manifest.json"
      provides: "Stable machine-readable manifest still emitted after the refactor"
  key_links:
    - from: "src/pdfmd/cli/run_quality_gate.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "CLI delegates quality-gate behavior to the package module"
      pattern: "from pdfmd.gates"
    - from: "src/pdfmd/cli/run_challenge_corpus.py"
      to: "src/pdfmd/gates/challenge_corpus.py"
      via: "CLI delegates challenge-corpus behavior to the package module"
      pattern: "from pdfmd.gates"
---

<objective>
Move gate and smoke-check implementations into `src/pdfmd/gates/` so the CLI layer becomes a thin compatibility surface and the current verification stack has a real subsystem home.

Purpose: make the gate layer easier to inspect, test, and evolve while keeping current report formats and failure semantics stable.
Output: real gate modules behind stable CLI entrypoints
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-extraction-and-wrapper-parity/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared gate primitives and report producers into `pdfmd.gates`</name>
  <files>src/pdfmd/gates/common.py, src/pdfmd/gates/audit.py, src/pdfmd/gates/probe.py, src/pdfmd/gates/regressions.py, src/pdfmd/gates/review_packet.py, src/pdfmd/cli/audit_bundle.py, src/pdfmd/cli/probe_artifacts.py, src/pdfmd/cli/check_regressions.py, src/pdfmd/cli/render_review_packet.py, src/pdfmd/cli/quality_gate_common.py</files>
  <action>
Move the reusable gate pieces out of the CLI layer into `src/pdfmd/gates/`.

This includes the core logic behind:
- audit
- artifact probing
- regression-scope resolution
- review packet rendering
- shared gate helpers

Leave the CLI files as thin entrypoints that delegate to the new package modules.

Preserve the current JSON/report schema and scope-resolution behavior exactly unless a test proves a necessary correction.
  </action>
  <verify>
Run:
```bash
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_regression_scopes.py' -v
python3 skills/pdf-to-structured-markdown/scripts/probe_artifacts.py generated/why-ethics >/tmp/pdfmd-probe.json
python3 skills/pdf-to-structured-markdown/scripts/check_regressions.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-regressions.json --strict
```
Verify that the tools still resolve scopes and emit structured output without import/path regressions.
  </verify>
  <done>Shared gate primitives live under `pdfmd.gates` with CLI parity preserved</done>
</task>

<task type="auto">
  <name>Task 2: Extract quality gate and challenge corpus orchestrators into `pdfmd.gates`</name>
  <files>src/pdfmd/gates/quality_gate.py, src/pdfmd/gates/challenge_corpus.py, src/pdfmd/cli/run_quality_gate.py, src/pdfmd/cli/run_challenge_corpus.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Move the orchestrator logic for:
- `run_quality_gate.py`
- `run_challenge_corpus.py`

into real `pdfmd.gates` modules.

Keep these invariants:
- manifests are still written
- challenge corpus remains soft-gate/report-only in the current project state
- `make gate` and `make smoke` still work through the skill wrappers
- known `why-ethics` failures remain classified as product/runtime issues, not path/import crashes

Update project-ops tests if needed to assert the post-split default paths still point at the skill layer and generated report locations.
  </action>
  <verify>
Run:
```bash
make smoke
make gate
```
`make gate` does not need to pass the product gate in this phase. It must, however, reach the real gate logic and produce updated runtime artifacts/report JSON instead of failing on packaging/path issues.
  </verify>
  <done>Quality-gate and challenge-corpus orchestrators live under `pdfmd.gates`, and wrapper commands still reach them correctly</done>
</task>

</tasks>

<verification>
- `src/pdfmd/gates/` contains real implementations, not only one-line aliases
- `make smoke` still succeeds in soft-gate mode
- `make gate` reaches the actual gate runtime and writes artifacts even if the underlying holdout still fails
- Gate path defaults stay aligned with the skill layer
</verification>

<success_criteria>
- Gate logic has moved out of the CLI layer
- Wrapper CLIs still behave correctly
- Known `why-ethics` failures stay classified as product/runtime failures rather than package-split regressions
- Report and manifest formats remain stable
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-extraction-and-wrapper-parity/02-03-SUMMARY.md`
</output>
