# Phase 01 Plan

1. Create root repo scaffolding and GSD state.
2. Add the package skeleton under `src/pdfmd/`.
3. Keep existing skill scripts as thin wrappers.
4. Add operator commands and basic manifest-aware tooling.
5. Add project-ops tests and validate the new surface.
