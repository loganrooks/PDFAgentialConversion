# Phase 01 Context

Goal: formalize the repo as a Git + GSD workspace with a canonical local operator surface and runtime-output policy.

Acceptance:
- root docs exist
- `.planning/` matches GSD expectations
- `generated/` is treated as runtime output
- `make status` and `make doctor` are usable
