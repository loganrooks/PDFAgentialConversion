---
phase: 05-cross-book-extractor-closure
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/pdfmd/convert/convert_pdf.py
  - src/pdfmd/convert/layout.py
  - src/pdfmd/convert/rag.py
  - src/pdfmd/convert/render.py
  - src/pdfmd/gates/probe.py
  - src/pdfmd/benchmarks/variant_comparison.py
  - skills/pdf-to-structured-markdown/references/cross-book-variants.json
  - skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py
  - skills/pdf-to-structured-markdown/references/of-grammatology-regressions.json
  - skills/pdf-to-structured-markdown/references/otherwise-than-being-regressions.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Cross-book prose-boundary repair must stay bounded to the remaining Of Grammatology and Otherwise than Being failure classes"
    - "Alternative heuristics must be compared explicitly, with why-ethics and Specters of Marx acting as veto gates"
    - "The simplest passing variant wins, and losing toggles must be removed after selection"
  artifacts:
    - path: "src/pdfmd/convert/rag.py"
      provides: "Bounded prose-fragment join and duplicate-boundary suppression logic for the remaining challenge-corpus defects"
      min_lines: 240
    - path: "src/pdfmd/benchmarks/variant_comparison.py"
      provides: "Variant comparison output that makes cross-book probe and challenge deltas easy to compare"
      min_lines: 220
    - path: "skills/pdf-to-structured-markdown/references/cross-book-variants.json"
      provides: "Named conservative/balanced/aggressive variants for prose-boundary and micro-region comparison"
      min_lines: 20
  key_links:
    - from: "skills/pdf-to-structured-markdown/references/cross-book-variants.json"
      to: "src/pdfmd/benchmarks/variant_comparison.py"
      via: "the comparison harness evaluates the exact bounded alternatives under Phase 05"
      pattern: "variant"
    - from: "src/pdfmd/convert/rag.py"
      to: "skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py"
      via: "accepted prose-boundary repairs are protected by fixture-driven tests"
      pattern: "continuation"
---

<objective>
Repair the remaining prose-boundary defects in Of Grammatology and Otherwise than Being using bounded local heuristics, explicit variant comparison, and hard why-ethics/Specters veto gates.

Purpose: close the active cross-book prose failure surface without reopening canonical-holdout or negative-control regressions.
Output: selected bounded prose-boundary repair, updated comparison artifacts, and reduced challenge-corpus defect counts within the Phase 05 thresholds
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-book-extractor-closure/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded prose-boundary repair alternatives and compare them explicitly</name>
  <files>src/pdfmd/convert/convert_pdf.py, src/pdfmd/convert/layout.py, src/pdfmd/convert/rag.py, src/pdfmd/convert/render.py, src/pdfmd/gates/probe.py, src/pdfmd/benchmarks/variant_comparison.py, skills/pdf-to-structured-markdown/references/cross-book-variants.json</files>
  <action>
Implement the bounded alternatives for the remaining cross-book prose defects and compare them on the real corpora before choosing a winner.

The comparison set must cover:
- `boundary_overlap`: `conservative`, `balanced`, `aggressive`
- `prose_join`: `conservative`, `balanced`, `aggressive`
- `micro_region_mode`: `reclassify_first`, `group_first`

Selection rule:
- discard any variant that fails why-ethics
- discard any variant that changes Specters of Marx
- among survivors, prefer the one with the best Of Grammatology and Otherwise than Being reductions
- if equivalent, choose the simpler and then faster variant

Remove losing toggles after the winner is chosen.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/compare_variants.py skills/pdf-to-structured-markdown/references/cross-book-variants.json --gate-mode soft --force
make gate
python3 skills/pdf-to-structured-markdown/scripts/run_challenge_corpus.py skills/pdf-to-structured-markdown/references/challenge-corpus.json --gate-mode soft --force
```
Verify that the accepted variant keeps why-ethics green, leaves Specters of Marx unchanged, and improves the active books relative to the current cross-book baseline.
  </verify>
  <done>The bounded prose-boundary repair is chosen by measured comparison rather than intuition</done>
</task>

<task type="auto">
  <name>Task 2: Lock the accepted prose-boundary behavior into regressions and threshold checks</name>
  <files>skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py, skills/pdf-to-structured-markdown/references/of-grammatology-regressions.json, skills/pdf-to-structured-markdown/references/otherwise-than-being-regressions.json</files>
  <action>
Once the winning variant is chosen, promote the repaired cross-book cases into fixture and regression coverage.

Acceptance targets for the repaired outputs:
- `Of Grammatology`
  - no overlap warning
  - `rag_block_lowercase_start <= 13`
  - `rag_block_dangling_end <= 2`
  - `rag_block_hyphen_end = 0`
  - `repeated_adjacent_word = 0`
  - max atomic chunk `<= 1600`
- `Otherwise than Being`
  - no audit code beyond `high_complex_layout_ratio`
  - no overlap warning
  - `rag_block_lowercase_start <= 9`
  - `rag_block_dangling_end <= 1`
  - `rag_block_hyphen_end = 0`
  - `repeated_adjacent_word <= 1`
  - max atomic chunk `<= 1600`
  - `boundary_page_many_micro_regions` either `0` or defensibly reclassified out of prose and protected by regression
  </action>
  <verify>
Run:
```bash
make test-fast
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_prose_fragment_repairs.py' -v
python3 skills/pdf-to-structured-markdown/scripts/check_regressions.py generated/of-grammatology skills/pdf-to-structured-markdown/references/of-grammatology-regressions.json --strict
python3 skills/pdf-to-structured-markdown/scripts/check_regressions.py generated/otherwise-than-being skills/pdf-to-structured-markdown/references/otherwise-than-being-regressions.json --strict
make gate
make smoke
```
Verify that the active cross-book books meet the target thresholds while why-ethics remains green and Specters of Marx stays unchanged.
  </verify>
  <done>The accepted prose-boundary repair is protected by tests and source-specific regressions</done>
</task>

</tasks>

<verification>
- Why-ethics remains green throughout variant comparison
- Specters of Marx remains unchanged
- Of Grammatology and Otherwise than Being meet the stated threshold targets
- Losing heuristic variants are removed after selection
</verification>

<success_criteria>
- The remaining cross-book prose failure surface is materially reduced
- Alternatives were compared explicitly rather than chosen by intuition
- The simplest passing variant is locked into tests and regressions
- Why-comment remains deferred to Phase 06
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-book-extractor-closure/05-03-SUMMARY.md`
</output>
