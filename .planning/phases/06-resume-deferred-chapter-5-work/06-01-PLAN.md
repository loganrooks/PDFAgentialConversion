---
phase: 06-resume-deferred-chapter-5-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/pdf-to-structured-markdown/references/why-ethics-regressions.json
  - skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
  - skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py
  - skills/pdf-to-structured-markdown/tests/test_regression_scopes.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phase 06 must lock the exact 7c/7d source truth before any extractor behavior changes are accepted"
    - "The deferred chapter-5 repair stays bounded to the why-comment inset-quote class; no page-map, ToC, filename, or embedding-projection work belongs here"
    - "Manual packet acceptance cannot be enforced until the target scopes are protected by strict regressions and explicit checklist coverage"
  artifacts:
    - path: "skills/pdf-to-structured-markdown/references/why-ethics-regressions.json"
      provides: "Strict anchored truth for the unresolved 7c/7d chapter-5 scopes"
      min_lines: 120
    - path: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      provides: "Manual-sample target set that explicitly covers the chapter-5 scopes to be repaired"
      min_lines: 200
    - path: "skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py"
      provides: "Phase-specific characterization coverage for the page-local inset-quote class"
      min_lines: 200
  key_links:
    - from: "skills/pdf-to-structured-markdown/references/why-ethics-regressions.json"
      to: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      via: "the same 7c/7d truths protected by regressions must also appear in the manual acceptance packet"
      pattern: "7c"
    - from: "skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py"
      to: "skills/pdf-to-structured-markdown/references/why-ethics-regressions.json"
      via: "fixture coverage and strict regressions must describe the same page-local failure class"
      pattern: "why-comment"
---

<objective>
Lock the exact chapter-5 truth packet for the deferred `why-comment` repair before any extractor changes land.

Purpose: replace the current report-only `7c/7d` knowledge with strict, source-anchored executable expectations.
Output: exact 7c/7d regressions, explicit manual-sample target coverage, and characterization fixtures that bound the repair surface
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resume-deferred-chapter-5-work/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the unresolved 7c/7d truths into strict regressions</name>
  <files>skills/pdf-to-structured-markdown/references/why-ethics-regressions.json, skills/pdf-to-structured-markdown/tests/test_regression_scopes.py</files>
  <action>
Use the current page images, review packet evidence, and existing sidecar outputs to lock the exact chapter-5 truth for the remaining unresolved scopes.

At minimum:
- verify and add strict regressions for:
  - `7c Citation`
  - `7c Commentary`
  - `7d Commentary`
- keep the existing repaired `6a`, `6c`, and `7a` regressions intact
- ensure repeated-label scope resolution remains stable when these new checks are added

Do not "fix the test to the current broken output." If a target is still wrong in the bundle, the new regression should fail until the extractor is repaired.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/check_regressions.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-regressions.json --strict
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_regression_scopes.py' -v
```
Verify that the new 7c/7d checks are exercised explicitly and fail only for the still-unrepaired target class.
  </verify>
  <done>The unresolved chapter-5 scopes are protected by strict regressions before any repair code lands</done>
</task>

<task type="auto">
  <name>Task 2: Tighten manual-sample target coverage for chapter 5</name>
  <files>skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json, skills/pdf-to-structured-markdown/tests/test_prose_fragment_repairs.py</files>
  <action>
Make sure the manual packet reflects the actual Phase 06 target set rather than only the currently failing subset.

At minimum:
- ensure the manual sample explicitly covers:
  - `7c Citation`
  - `7c Commentary`
  - `7d Commentary`
- keep the packet size and composition stable enough that Phase 04/05 evidence is still comparable
- add phase-specific fixture coverage for the inset-quote / note-block class and at least one negative control that must remain untouched

Do not enable acceptance enforcement in this task. This is still characterization and packet tightening.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/render_review_packet.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_prose_fragment_repairs.py' -v
```
Verify that the rendered packet names the intended chapter-5 targets explicitly and that the negative control fixture remains clean.
  </verify>
  <done>The chapter-5 repair surface is fully characterized in both strict regressions and the manual packet before implementation begins</done>
</task>

</tasks>

<verification>
- The why-ethics regression spec contains strict target checks for `7c Citation`, `7c Commentary`, and `7d Commentary`
- The manual packet explicitly covers the target scopes that Phase 06 is supposed to repair
- Characterization fixtures describe the page-local inset-quote class plus a negative control
- No extractor behavior change is required to complete this plan
</verification>

<success_criteria>
- Phase 06 implementation starts from exact source truth instead of report-only memory
- The remaining chapter-5 target class is bounded clearly enough to reject accidental overfitting
- Manual acceptance has a real target set to enforce later
</success_criteria>

<output>
After completion, create `.planning/phases/06-resume-deferred-chapter-5-work/06-01-SUMMARY.md`
</output>
