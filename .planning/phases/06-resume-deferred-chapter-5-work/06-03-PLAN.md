---
phase: 06-resume-deferred-chapter-5-work
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/gates/review_packet.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phase 06 is not complete until manual packet acceptance becomes an enforced part of the canonical gate, not just a report appendix"
    - "Turning on manual enforcement must preserve the Phase 04 two-run stability discipline for the canonical holdout"
    - "The challenge corpus hard gate must remain green when the canonical manual gate is promoted"
  artifacts:
    - path: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      provides: "Canonical gate configuration with manual acceptance enforcement enabled"
      min_lines: 200
    - path: "src/pdfmd/gates/quality_gate.py"
      provides: "Gate behavior that treats manual acceptance as a real hard-gate condition"
      min_lines: 500
    - path: "src/pdfmd/gates/review_packet.py"
      provides: "Review-packet output that remains explicit and diagnosable once manual enforcement is active"
      min_lines: 120
  key_links:
    - from: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "manual_sample.enforce_acceptance becomes a real canonical pass/fail condition"
      pattern: "enforce_acceptance"
    - from: "src/pdfmd/gates/quality_gate.py"
      to: "generated/why-ethics/quality-gate/gate-runtime.json"
      via: "the manual gate promotion must still produce stable repeated gate signatures"
      pattern: "stability_runs"
---

<objective>
Promote the repaired chapter-5 packet from report-only review into an enforced part of the canonical gate, and prove that the canonical holdout remains stable.

Purpose: make the `why-comment` repair part of the actual acceptance contract.
Output: manual packet acceptance enforced in the why-ethics gate, plus repeated successful gate proof on the repaired state
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resume-deferred-chapter-5-work/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce manual packet acceptance in the canonical gate</name>
  <files>skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json, src/pdfmd/gates/quality_gate.py, src/pdfmd/gates/review_packet.py</files>
  <action>
Turn the repaired chapter-5 manual packet into a real canonical gate instead of a report-only appendix.

At minimum:
- set `manual_sample.enforce_acceptance` to `true`
- keep the packet diagnostics explicit enough that a future failure still names the target scope cleanly
- do not weaken the existing structural, probe, retrieval, embedding, or challenge-corpus gates

This task is a promotion step, not another extractor-repair step.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/run_quality_gate.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json --stability-runs 1
python3 skills/pdf-to-structured-markdown/scripts/render_review_packet.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
```
Verify that the manual packet passes with no `fail` verdicts and that the gate remains green with enforcement turned on.
  </verify>
  <done>Manual packet acceptance is now a real why-ethics gate condition</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove the canonical holdout under the repaired-and-enforced state</name>
  <files>skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Reapply the Phase 04 trust rule now that the canonical gate contract has changed: require repeated successful runs with identical signatures.

At minimum:
- rerun `make gate` with `stability_runs=2`
- rerun the hard challenge corpus gate
- confirm `Specters of Marx` remains clean and the accepted residual challenge thresholds still hold

Do not re-baseline anything in this task. This is proof, not threshold editing.
  </action>
  <verify>
Run:
```bash
make gate GATE_ARGS='--stability-runs 2'
make smoke
make status
```
Verify that the canonical why-ethics gate stays green with manual enforcement on and that the repeated-run signatures remain identical.
  </verify>
  <done>The repaired-and-enforced why-ethics gate is proven stable, and the hard challenge corpus still passes</done>
</task>

</tasks>

<verification>
- Manual acceptance is enforced in the why-ethics gate
- The full packet passes with no `fail` verdicts
- Two consecutive gate runs succeed with identical signatures on the repaired state
- The hard challenge corpus remains green
</verification>

<success_criteria>
- The final why-comment repair becomes part of the real canonical contract
- The project does not rely on report-only chapter-5 success anymore
- The enforced repaired state is stable enough to close the milestone
</success_criteria>

<output>
After completion, create `.planning/phases/06-resume-deferred-chapter-5-work/06-03-SUMMARY.md`
</output>
