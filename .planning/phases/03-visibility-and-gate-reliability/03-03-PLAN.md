---
phase: 03-visibility-and-gate-reliability
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pdfmd/ops/status_snapshot.py
  - src/pdfmd/ops/doctor.py
  - src/pdfmd/common/paths.py
  - src/pdfmd/common/runtime.py
  - README.md
  - Makefile
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Project health is understandable at a glance from `make status` plus the root README"
    - "Doctor and status surfaces distinguish current failures from stale artifacts and from optional experiment output"
    - "Remote SSH/GPU readiness is visible without making the remote backend part of the canonical pass/fail path"
  artifacts:
    - path: "src/pdfmd/ops/status_snapshot.py"
      provides: "Concise project health snapshot driven by authoritative manifests and runtime artifacts"
      min_lines: 120
    - path: "src/pdfmd/ops/doctor.py"
      provides: "Environment readiness report for local Apple gate and optional remote experiment backend"
      min_lines: 120
    - path: "README.md"
      provides: "Operator-facing overview with current state, directory map, commands, and tracked-vs-runtime outputs"
      min_lines: 40
  key_links:
    - from: "src/pdfmd/ops/status_snapshot.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "status reads authoritative gate manifests and runtime diagnostics instead of guessing from file presence"
      pattern: "quality-gate"
    - from: "src/pdfmd/ops/doctor.py"
      to: "src/pdfmd/benchmarks/remote_backends.py"
      via: "doctor surfaces optional SSH remote readiness without coupling it to local gate success"
      pattern: "remote"
---

<objective>
Make the project state legible at a glance so operators can see what is green, what is stale, and what remains blocked before running deeper checks.

Purpose: reduce ambiguity and operator anxiety as the codebase and verification surface grow.
Output: trustworthy status/doctor views and refreshed root operator docs
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visibility-and-gate-reliability/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh status snapshot to reflect authoritative runtime and gate state</name>
  <files>src/pdfmd/ops/status_snapshot.py, src/pdfmd/common/paths.py, src/pdfmd/common/runtime.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Improve the status snapshot so it reads the newest authoritative manifests and runtime artifacts and summarizes:
- current roadmap phase
- latest why-ethics gate status
- latest challenge-corpus smoke status
- latest backend-comparison status
- active known failures
- artifact freshness or staleness
- latest timestamps for bundle/gate/report outputs

Keep the output concise. It should help an operator orient quickly, not become another long report.
  </action>
  <verify>
Run:
```bash
make status
python3 skills/pdf-to-structured-markdown/scripts/status_snapshot.py
```
Verify that stale or missing artifacts are rendered explicitly and that the current active failures match known state instead of inferred guesses.
  </verify>
  <done>Status surface is concise, current, and grounded in authoritative artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Refresh doctor output and root operator docs</name>
  <files>src/pdfmd/ops/doctor.py, README.md, Makefile, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Make `make doctor` and the root README the canonical at-a-glance operator onboarding surface.

Doctor should clearly report:
- local Python/runtime version
- Swift availability
- Apple helper readiness
- optional SSH remote backend readiness
- any obvious missing prerequisites

README should clearly document:
- top-level directory map
- canonical commands
- tracked vs runtime-only outputs
- the current known active failures
- where the hard holdout and soft smoke reports live
  </action>
  <verify>
Run:
```bash
make doctor
make status
```
Verify that a fresh operator can understand the project state from README plus the two commands without digging through planning docs.
  </verify>
  <done>Doctor and README give a reliable at-a-glance view of local and optional remote readiness</done>
</task>

</tasks>

<verification>
- `make status` is concise and freshness-aware
- `make doctor` surfaces local and remote readiness clearly
- README matches the actual operator flow and current gate tiers
- Operator-surface tests cover the new visibility behavior
</verification>

<success_criteria>
- Project health is understandable at a glance
- Status and doctor surfaces distinguish stale, failed, skipped, and current artifacts
- Remote readiness is visible but still optional
- Root docs stay aligned with the actual local command surface
</success_criteria>

<output>
After completion, create `.planning/phases/03-visibility-and-gate-reliability/03-03-SUMMARY.md`
</output>
