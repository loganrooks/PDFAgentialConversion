---
phase: 03-visibility-and-gate-reliability
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - .planning/STATE.md
  - .planning/codebase/overview.md
  - .planning/codebase/entrypoints.md
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/gates/challenge_corpus.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
  - skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phase 03 ends with an explicit verification protocol rather than a vague claim that the runtime is 'better'"
    - "The project state and codebase map stay up to date as the operator and gate surfaces evolve"
    - "Phase 03 can complete while Phase 04 product-fix work remains intentionally unresolved"
  artifacts:
    - path: ".planning/codebase/overview.md"
      provides: "Updated at-a-glance codebase map reflecting the current subsystem and operator boundaries"
      min_lines: 40
    - path: ".planning/codebase/entrypoints.md"
      provides: "Updated entrypoint map for wrappers, package modules, and canonical operator commands"
      min_lines: 40
    - path: "skills/pdf-to-structured-markdown/tests/test_project_ops.py"
      provides: "Regression coverage for phase-level visibility and runtime verification behavior"
      min_lines: 240
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "state reflects the completed visibility/runtime work while preserving the next product-focus phase boundary"
      pattern: "Phase 03"
    - from: ".planning/codebase/entrypoints.md"
      to: "README.md"
      via: "operator docs and codebase map agree on the canonical command and module surfaces"
      pattern: "make"
---

<objective>
Close Phase 03 with explicit verification, refreshed codebase visibility, and a clean handoff into Phase 04 holdout recovery.

Purpose: avoid finishing the visibility/runtime phase with stale docs, ambiguous status, or unverified assumptions about what is and is not fixed.
Output: end-of-phase verification artifacts, updated codebase map, and state that clearly hands off to Phase 04
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visibility-and-gate-reliability/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify runtime stability protocol and preserve the phase boundary</name>
  <files>src/pdfmd/gates/quality_gate.py, src/pdfmd/gates/challenge_corpus.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Finish the phase with an explicit verification protocol for visibility and runtime reliability.

At minimum:
- document and exercise the two-consecutive-identical-summary check for the why-ethics gate
- verify that challenge-corpus smoke behavior remains soft-gate and report-only
- make sure any remaining failures are clearly recorded as product-level or later-phase work

Do not use this plan to repair the why-ethics probe/retrieval/product failures themselves. The handoff to Phase 04 must remain clear.
  </action>
  <verify>
Run:
```bash
make test
make status
make smoke
make compare-backends
make gate
```
Verify that Phase 03 can distinguish runtime reliability from unresolved holdout quality problems, even if the latter remain red.
  </verify>
  <done>Phase 03 ends with explicit runtime verification and a clean boundary into Phase 04</done>
</task>

<task type="auto">
  <name>Task 2: Refresh codebase map, docs, and state for the next phase</name>
  <files>.planning/STATE.md, .planning/codebase/overview.md, .planning/codebase/entrypoints.md, README.md, skills/pdf-to-structured-markdown/tests/test_wrapper_parity.py</files>
  <action>
Refresh the codebase visibility artifacts now that Phase 02 package extraction and Phase 03 visibility/runtime work are in place.

Update:
- codebase overview
- entrypoint map
- state handoff
- any README references that still imply stale pre-phase behavior

The result should let a fresh operator understand:
- where the product code lives
- where the wrapper surface lives
- how the gate tiers work
- what Phase 04 is responsible for next
  </action>
  <verify>
Run:
```bash
make map
make status
```
Verify that the codebase map and status output agree on the current structure and next active phase.
  </verify>
  <done>Phase state, codebase map, and operator docs are synchronized for the Phase 04 handoff</done>
</task>

</tasks>

<verification>
- Phase 03 ends with explicit runtime/visibility verification, not just implementation
- Codebase map and operator docs reflect the current module and wrapper structure
- Remaining failures are clearly attributed to later product phases
- The next phase handoff is unambiguous
</verification>

<success_criteria>
- Visibility and runtime reliability work is explicitly verified
- State/docs/codebase map are up to date
- Phase 04 starts from a clean, well-instrumented baseline
- No Phase 04 product fixes are smuggled into Phase 03 close-out
</success_criteria>

<output>
After completion, create `.planning/phases/03-visibility-and-gate-reliability/03-04-SUMMARY.md`
</output>
