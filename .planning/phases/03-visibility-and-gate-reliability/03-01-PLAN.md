---
phase: 03-visibility-and-gate-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pdfmd/common/manifests.py
  - src/pdfmd/common/io.py
  - src/pdfmd/common/runtime.py
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/gates/challenge_corpus.py
  - src/pdfmd/benchmarks/remote_backends.py
  - src/pdfmd/convert/output.py
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every report-producing command writes one authoritative machine-readable manifest rather than relying on ad hoc JSON side effects"
    - "Runtime failures are classified explicitly and do not cascade into misleading downstream gate failures"
    - "Generated runtime artifacts can be distinguished as fresh, stale, missing, or failed from manifest and runtime metadata alone"
  artifacts:
    - path: "src/pdfmd/common/manifests.py"
      provides: "Shared manifest schema helpers for bundle generation, quality gate, challenge corpus, and backend comparison"
      min_lines: 40
    - path: "src/pdfmd/gates/quality_gate.py"
      provides: "Gate runtime classification that short-circuits embedding metrics on runtime failure"
      min_lines: 220
    - path: "skills/pdf-to-structured-markdown/tests/test_project_ops.py"
      provides: "Contract coverage for manifests, freshness markers, and runtime failure classification"
      min_lines: 220
  key_links:
    - from: "src/pdfmd/common/manifests.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "quality-gate artifacts are validated through the shared manifest contract"
      pattern: "ensure_manifest_payload"
    - from: "src/pdfmd/common/manifests.py"
      to: "src/pdfmd/gates/challenge_corpus.py"
      via: "challenge-corpus reports and manifests share the same contract surface"
      pattern: "ensure_manifest_payload"
    - from: "src/pdfmd/common/runtime.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "runtime outcomes are normalized through shared helpers before status/report rendering"
      pattern: "runtime"
---

<objective>
Make runtime artifacts authoritative before tuning gate behavior: manifests must be standardized, runtime failures must classify cleanly, and stale artifact state must not be mistaken for current gate outcomes.

Purpose: restore measurement integrity so later runtime calibration and holdout recovery work from trustworthy signals.
Output: standardized manifests, explicit runtime failure classification, freshness-aware artifact contracts
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visibility-and-gate-reliability/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize manifest payloads and freshness markers across runtime producers</name>
  <files>src/pdfmd/common/manifests.py, src/pdfmd/common/io.py, src/pdfmd/gates/challenge_corpus.py, src/pdfmd/benchmarks/remote_backends.py, src/pdfmd/convert/output.py</files>
  <action>
Extend the shared manifest layer so all runtime producers write a consistent schema that includes enough metadata to reason about freshness and provenance:
- bundle generation
- quality gate
- challenge corpus
- backend comparison

At minimum, preserve backward-compatible existing fields and add standardized fields for:
- run timestamp
- artifact type
- input hash or bundle hash where applicable
- config hash where applicable
- experiment/variant id where applicable
- freshness or generation status markers when the producer is allowed to skip work

Do not change report meaning or gate thresholds in this task. The goal is to make the runtime outputs trustworthy and comparable, not to change product behavior.
  </action>
  <verify>
Run:
```bash
make test-fast
python3 -m unittest discover -s skills/pdf-to-structured-markdown/tests -p 'test_project_ops.py' -v
```
Verify that manifest contract tests cover all current runtime report types and fail fast on schema drift.
  </verify>
  <done>All runtime report producers share one manifest contract with explicit provenance and freshness markers</done>
</task>

<task type="auto">
  <name>Task 2: Classify runtime failures cleanly and prevent misleading downstream gate errors</name>
  <files>src/pdfmd/common/runtime.py, src/pdfmd/gates/quality_gate.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Tighten the quality-gate runtime path so embedding-runtime failures behave as one classified gate failure rather than a cascade of follow-on errors.

Specifically:
- short-circuit embedding metric evaluation when the embedding helper times out, crashes, or yields invalid runtime artifacts
- emit one explicit runtime failure classification
- record whether cleanup succeeded for helper processes
- make stale runtime artifacts distinguishable from current-run artifacts

Do not change the canonical Apple-backed gate policy. This task is about error semantics and operator trustworthiness.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/run_quality_gate.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
```
If the embedding helper still fails, verify that the gate report shows one classified runtime failure and does not emit misleading "missing summary" cascades.
  </verify>
  <done>Runtime failures are classified once, cleanup is recorded, and downstream gate reporting stays trustworthy</done>
</task>

</tasks>

<verification>
- All current report directories include a validated run manifest
- Runtime failures are classified explicitly rather than inferred from missing downstream files
- Manifest and runtime schema tests fail fast if fields drift
- No existing CLI contract is broken while manifest authority is tightened
</verification>

<success_criteria>
- Manifests are standardized across runtime producers
- Fresh vs stale vs skipped vs failed artifact states are machine-readable
- Gate runtime failures do not cascade into misleading downstream failures
- Contract tests cover the runtime artifact surface
</success_criteria>

<output>
After completion, create `.planning/phases/03-visibility-and-gate-reliability/03-01-SUMMARY.md`
</output>
