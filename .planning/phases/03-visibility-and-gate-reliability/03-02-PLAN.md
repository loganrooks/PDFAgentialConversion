---
phase: 03-visibility-and-gate-reliability
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/pdfmd/benchmarks/calibration.py
  - src/pdfmd/benchmarks/calibrate_embedding_timeout.py
  - src/pdfmd/gates/quality_gate.py
  - src/pdfmd/cli/run_quality_gate.py
  - skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json
  - skills/pdf-to-structured-markdown/tests/test_project_ops.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Apple embedding timeout settings are calibrated from measured local runtimes on the M4 instead of guessed constants"
    - "The quality gate records runtime duration, attempt count, timeout class, and helper cleanup results for every embedding run"
    - "Phase 03 verifies runtime reliability without claiming that holdout quality metrics are already fixed"
  artifacts:
    - path: "src/pdfmd/benchmarks/calibration.py"
      provides: "Measured timeout calibration over repeated embedding runs with a deterministic suggestion rule"
      min_lines: 80
    - path: "src/pdfmd/gates/quality_gate.py"
      provides: "Gate runtime integration that consumes calibrated timeout settings and records stability-run signatures"
      min_lines: 260
    - path: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      provides: "Canonical runtime gate configuration with calibrated or calibration-ready embedding settings"
      min_lines: 40
  key_links:
    - from: "src/pdfmd/benchmarks/calibration.py"
      to: "src/pdfmd/gates/quality_gate.py"
      via: "measured timeout suggestions feed the runtime gate rather than living as a separate experiment"
      pattern: "timeout"
    - from: "skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json"
      to: "src/pdfmd/cli/run_quality_gate.py"
      via: "runtime gate settings come from the canonical gate config"
      pattern: "runtime_gates"
---

<objective>
Calibrate and harden the Apple embedding runtime path so the local gate can complete predictably on the M4 and report runtime behavior with trustworthy diagnostics.

Purpose: make the canonical local gate operational as a runtime system before Phase 04 tries to restore holdout metrics.
Output: timeout calibration, runtime diagnostics, and stability-run protocol integrated into the quality gate
</objective>

<execution_context>
@/Users/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/Users/rookslog/.codex/get-shit-done-reflect/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visibility-and-gate-reliability/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibrate Apple embedding timeout from repeated local measurements</name>
  <files>src/pdfmd/benchmarks/calibration.py, src/pdfmd/benchmarks/calibrate_embedding_timeout.py, skills/pdf-to-structured-markdown/references/why-ethics-quality-gate.json</files>
  <action>
Use the existing calibration support to make timeout selection an explicit measured process on the local M4.

Implement and verify a calibration workflow that:
- runs the embedding evaluation 5 times on the canonical why-ethics benchmark
- records per-run durations and outcome classes
- computes the suggested timeout using the agreed rule:
  - `max(p95 * 2, p99 + 30s)` rounded up
- stores the measured calibration artifact in a stable report location
- keeps the gate config as the single source of truth for runtime settings

If calibration cannot complete because the helper remains unstable, record that clearly as the phase outcome rather than faking a calibrated value.
  </action>
  <verify>
Run:
```bash
python3 skills/pdf-to-structured-markdown/scripts/calibrate_embedding_timeout.py generated/why-ethics skills/pdf-to-structured-markdown/references/why-ethics-retrieval-benchmark.json --runs 5
```
Verify that the calibration output includes durations, failure classes when relevant, and a machine-readable timeout recommendation.
  </verify>
  <done>Apple embedding timeout is measured from repeated local runs or explicitly blocked by classified runtime instability</done>
</task>

<task type="auto">
  <name>Task 2: Integrate runtime diagnostics and two-run stability signatures into the quality gate</name>
  <files>src/pdfmd/gates/quality_gate.py, src/pdfmd/cli/run_quality_gate.py, skills/pdf-to-structured-markdown/tests/test_project_ops.py</files>
  <action>
Extend the quality gate so runtime verification is explicit and repeatable.

At minimum:
- persist gate-runtime and embedding-runtime artifacts for every run
- include compact summary fields:
  - success/failure
  - timeout class
  - wall-clock duration
  - attempt count
  - orphan-process cleanup result
- support the two-consecutive-identical-summary check once runtime calibration is available
- keep runtime reliability separate from product metric recovery

Do not change Phase 04's acceptance thresholds here. This plan only makes the runtime verification surface explicit and stable.
  </action>
  <verify>
Run:
```bash
make gate
```
If the full gate still fails for known product reasons, verify that runtime artifacts are present, helper cleanup is recorded, and repeated runs can be compared by summary signature.
  </verify>
  <done>Quality-gate runtime diagnostics are explicit, repeatable, and ready for two-run stability verification</done>
</task>

</tasks>

<verification>
- Calibration output is machine-readable and based on repeated local runs
- Runtime artifacts record wall-clock, attempt count, failure class, and cleanup result
- The gate can compare summary signatures across runs without hand inspection
- Any remaining gate failure after this plan is clearly distinguishable as runtime or product-level
</verification>

<success_criteria>
- Apple timeout selection is measured rather than guessed
- Runtime diagnostics are authoritative and comparable across runs
- Two-run stability verification is supported
- The gate runtime surface is ready for Phase 04 holdout recovery work
</success_criteria>

<output>
After completion, create `.planning/phases/03-visibility-and-gate-reliability/03-02-SUMMARY.md`
</output>
