# Requirements Archive: v1.0 Foundation, Visibility, and Gate Recovery

**Archived:** 2026-03-16
**Status:** SHIPPED

This milestone closed with all tracked infrastructure requirements satisfied.

---

## Outcome Summary

- Requirements satisfied: `7/7`
- Critical blockers: `0`
- Audit status: `passed`

## Requirement Outcomes

- [x] The repo must be understandable at a glance from root docs plus a single status command.
  Outcome: satisfied by the root docs, `make status`, and the status snapshot operator surface.
- [x] Existing skill script CLIs must remain backward-compatible while implementation moves into `src/pdfmd/`.
  Outcome: satisfied by the wrapper-parity layer and its regression tests.
- [x] `generated/` must be treated as runtime output, not the primary tracked source of truth.
  Outcome: satisfied by repo policy, ignore rules, and the tracked-baseline approach.
- [x] The canonical local verification path must remain Mac-first and Apple-embedding-backed.
  Outcome: satisfied by the calibrated Apple-backed `why-ethics` gate.
- [x] Remote SSH/GPU runs must stay report-only until explicitly promoted.
  Outcome: satisfied; remote backend comparison exists but remains outside canonical pass/fail.
- [x] The project must expose clear verification tiers.
  Outcome: satisfied by `make test-fast`, `make test`, `make gate`, `make smoke`, `make compare-backends`, and `make verify-all`.
- [x] Manifest drift must fail fast through explicit tests.
  Outcome: satisfied by the manifest contract tests in the project ops suite.

## Archived Source

# REQUIREMENTS

## Must-Haves

1. The repo must be understandable at a glance from root docs plus a single status command.
2. Existing skill script CLIs must remain backward-compatible while implementation moves into `src/pdfmd/`.
3. `generated/` must be treated as runtime output, not the primary tracked source of truth.
4. The canonical local verification path must remain Mac-first and Apple-embedding-backed.
5. Remote SSH/GPU runs must stay report-only until explicitly promoted.
6. The project must expose clear verification tiers:
   - fast local checks
   - canonical holdout gate
   - challenge-corpus smoke checks
   - optional remote experiments
7. Manifest drift must fail fast through explicit tests.

## Acceptance For This Infrastructure Milestone

- `.planning/` exists and reflects the current milestone.
- Root docs and operator commands exist.
- A real `src/pdfmd` package exists.
- Status and doctor tooling work locally.
- Current test suite remains green.
